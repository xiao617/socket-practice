export type MousePosition = {
    left: number,
    top: number
}
export type MouseEventObject = {
    clientX: number,
    clientY: number
}
export type SocketInfo = {
    rgb: string,
    left: number,
    top: number
}
export type ChatObject = {
    userId: string,
    msg: string,
    time: string
}